<%= form_with(model: overlay_field, local: true) do |form| %>
  <% if overlay_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(overlay_field.errors.count, "error") %> prohibited this overlay_field from being saved:</h2>

      <ul>
      <% overlay_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :overlay %>
    <%= form.text_field :overlay %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
