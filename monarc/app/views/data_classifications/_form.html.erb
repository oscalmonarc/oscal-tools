<%= form_with(model: data_classification, local: true) do |form| %>
  <% if data_classification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_classification.errors.count, "error") %> prohibited this data_classification from being saved:</h2>

      <ul>
      <% data_classification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
