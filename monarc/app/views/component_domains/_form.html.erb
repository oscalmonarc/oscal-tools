<%= form_with(model: component_domain, local: true) do |form| %>
  <% if component_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component_domain.errors.count, "error") %> prohibited this component_domain from being saved:</h2>

      <ul>
      <% component_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :component_id %>
    <%= form.text_field :component_id %>
  </div>

  <div class="field">
    <%= form.label :domain_environment_id %>
    <%= form.text_field :domain_environment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
